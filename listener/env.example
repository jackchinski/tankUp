BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY
# Optional: adjust polling interval for HTTP provider (default 4000)
POLLING_INTERVAL_MS=3000

# Optional: WebSocket endpoint (recommended to avoid HTTP filter eviction)
# If provided, listener will use WebSocket subscriptions
# BASE_WS_URL=wss://base-mainnet.g.alchemy.com/v2/YOUR_KEY

